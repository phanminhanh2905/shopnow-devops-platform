apiVersion: v1
kind: LimitRange
metadata:
  name: shopnow-limitrange
  namespace: shopnow
spec:
  limits:
  - type: Container
    default:
      cpu: "500m"
      memory: "512Mi"
    defaultRequest:
      cpu: "100m"
      memory: "256Mi"
